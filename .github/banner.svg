<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1280 400" width="1280" height="400" role="img" aria-label="tracktour — rank every track on an album">
  <defs>
    <radialGradient id="glow" cx="50%" cy="0%" r="75%">
      <stop offset="0%" stop-color="#1fca97" stop-opacity="0.22"/>
      <stop offset="45%" stop-color="#1fca97" stop-opacity="0.07"/>
      <stop offset="100%" stop-color="#1fca97" stop-opacity="0"/>
    </radialGradient>
    <style>
      /* Same technique as the in-app mark: fill-box with percentage origins, so
         a pivot is relative to the shape rather than to a coordinate system the
         parent transform has already scaled and shifted. The previous absolute
         origins (300px 286px and the like) sat far outside a tractor whose own
         box is 48x32, which is what threw it around the banner. */
      .rig, .wheels, .spin, .puff { transform-box: fill-box; }
      .rig { transform-origin: 50% 100%; animation: bob 500ms ease-in-out infinite; }
      .wheels { transform-origin: 50% 100%; animation: settle 500ms ease-in-out infinite; }
      .spin { transform-origin: 50% 50%; animation: roll 1000ms linear infinite; }
      /* The small wheel covers the same ground on a shorter circumference, so it
         turns faster by exactly the ratio of the radii (8.4 / 5.2). */
      .spin-front { animation-duration: 619ms; }

      .puff {
        transform-origin: 50% 50%;
        fill: #1fca97;
        opacity: 0;
        animation: puff 500ms ease-out infinite;
      }
      .puff-2 { animation-delay: 90ms; }
      .puff-3 { animation-delay: 180ms; }

      @keyframes bob {
        0%   { transform: scaleY(1)    scaleX(1); }
        30%  { transform: scaleY(1.18) scaleX(0.94); }
        55%  { transform: scaleY(1)    scaleX(1); }
        80%  { transform: scaleY(0.87) scaleX(1.06); }
        100% { transform: scaleY(1)    scaleX(1); }
      }
      @keyframes settle {
        0%, 55%, 100% { transform: scaleY(1); }
        80% { transform: scaleY(0.96); }
      }
      @keyframes roll { to { transform: rotate(360deg); } }
      @keyframes puff {
        0%   { opacity: 0.55; transform: translate(0, 0) scale(0.5); }
        100% { opacity: 0; transform: translate(-3px, -7px) scale(1.5); }
      }

      @media (prefers-reduced-motion: reduce) {
        .rig, .wheels, .spin, .puff { animation: none; }
        .puff { opacity: 0.35; }
      }
    </style>
  </defs>

  <rect width="1280" height="400" fill="#08080a"/>
  <rect width="1280" height="400" fill="url(#glow)"/>

  <!-- Tractor, at 6x the in-app mark -->
  <g transform="translate(78, 94) scale(6)">
    <g class="rig">
      <g class="smoke">
        <circle class="puff puff-1" cx="27" cy="3.4" r="1.9"/>
        <circle class="puff puff-2" cx="27" cy="3.4" r="1.5"/>
        <circle class="puff puff-3" cx="27" cy="3.4" r="1.2"/>
      </g>
      <rect x="25.6" y="4.6" width="2.6" height="11.4" rx="1.1" fill="#0c0c10"/>
      <path d="M10.5 16V5.6a1.6 1.6 0 0 1 1.6-1.6h8.6a1.6 1.6 0 0 1 1.5 1.1l2 9.4V16z" fill="#1fca97"/>
      <rect x="13" y="6.7" width="6.6" height="6.6" rx="1.2" fill="#0c0c10" opacity="0.55"/>
      <path d="M7.5 16h29.2a1.8 1.8 0 0 1 1.8 1.8v4.4a1.8 1.8 0 0 1-1.8 1.8H7.5z" fill="#1fca97"/>
      <rect x="30" y="18.4" width="7.4" height="2" rx="1" fill="#0c0c10"/>
    </g>
    <g class="wheels">
      <circle cx="34.5" cy="24.5" r="5.2" fill="#18181e" stroke="#2b2b34" stroke-width="1.4"/>
      <circle cx="34.5" cy="24.5" r="1.9" fill="#1fca97"/>
      <g class="spin spin-front" fill="#1fca97">
        <rect x="33.9" y="19.9" width="1.2" height="9.2" rx="0.6"/>
        <rect x="33.9" y="19.9" width="1.2" height="9.2" rx="0.6" transform="rotate(60 34.5 24.5)"/>
        <rect x="33.9" y="19.9" width="1.2" height="9.2" rx="0.6" transform="rotate(120 34.5 24.5)"/>
      </g>
      <circle cx="13.5" cy="21.5" r="8.4" fill="#18181e" stroke="#2b2b34" stroke-width="1.4"/>
      <circle cx="13.5" cy="21.5" r="3" fill="#1fca97"/>
      <g class="spin spin-rear" fill="#1fca97">
        <rect x="12.8" y="13.6" width="1.4" height="15.8" rx="0.7"/>
        <rect x="12.8" y="13.6" width="1.4" height="15.8" rx="0.7" transform="rotate(45 13.5 21.5)"/>
        <rect x="12.8" y="13.6" width="1.4" height="15.8" rx="0.7" transform="rotate(90 13.5 21.5)"/>
        <rect x="12.8" y="13.6" width="1.4" height="15.8" rx="0.7" transform="rotate(135 13.5 21.5)"/>
      </g>
    </g>
  </g>

  <!-- Set against the tractor's own centre, so the two read as one block. -->
  <g font-family="Inter, -apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, sans-serif">
    <text x="452" y="205" font-size="104" font-weight="800" letter-spacing="-5" fill="#f3f3f4">tracktour.</text>
    <text x="458" y="252" font-size="27" font-weight="500" fill="#a7a7b0">Rank every track on an album, two at a time.</text>
  </g>
</svg>
